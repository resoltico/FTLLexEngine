formatted-number = { NUMBER($amount) }
formatted-date = { DATETIME($date) }
with-options = { NUMBER($price, minimumFractionDigits: 2) }
datetime-options = { DATETIME($time, dateStyle: "long", timeStyle: "short") }
