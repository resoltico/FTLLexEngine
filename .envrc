# direnv configuration for FTLLexEngine
# Ensures all uv commands use the versioned venv, preventing the default
# .venv from being created alongside the scripts' .venv-3.13.
export UV_PROJECT_ENVIRONMENT=".venv-3.13"
